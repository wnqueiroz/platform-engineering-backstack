apiVersion: v1
kind: Service
metadata:
  name: crossview
  namespace: crossview-system
  labels:
    app: crossview
    backstage.io/kubernetes-id: crossview
    backstage.io/kubernetes-namespace: crossview-system
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: crossview
