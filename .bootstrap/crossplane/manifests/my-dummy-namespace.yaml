apiVersion: kubernetes.crossplane.io/v1alpha2
kind: Object
metadata:
  name: my-dummy-namespace
spec:
  forProvider:
    manifest:
      apiVersion: v1
      kind: Namespace
      metadata:
        name: my-dummy-namespace
        labels:
          example: "true"
  providerConfigRef:
    name: kubernetes-provider
