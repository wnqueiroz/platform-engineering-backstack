apiVersion: dynamodb.aws.upbound.io/v1beta1
kind: Table
metadata:
  generateName: crossplane-dynamodb-
spec:
  forProvider:
    region: us-east-1
    writeCapacity: 1
    readCapacity: 1
    attribute:
      - name: S3ID
        type: S
    hashKey: S3ID
  providerConfigRef:
    name: default
